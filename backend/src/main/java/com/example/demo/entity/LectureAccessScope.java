package com.example.demo.entity;

/**
 * PRIVATE content is visible only to its owner/collaborators. CLASS content may
 * be distributed by the Class service after it validates an active enrollment.
 */
public enum LectureAccessScope {
    PRIVATE,
    CLASS
}
